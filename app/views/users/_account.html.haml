.panel.panel-primary
  .panel-heading
    %h3= t ".title"
  .panel-body
    .row
      .col-md-12
        %table.table.table-striped
          %tbody
            %tr.total
              %td= t ".total"
              %td.text-right
                = number_to_currency current_user.account_sum
            - current_user.wallets.each do |wallet|
              %tr
                %td= wallet.name
                %td.text-right
                  = number_to_currency wallet.balance
        %table.table.table-striped
          %tbody
            %tr.danger
              %td= t ".limit"
              %td.text-right
                = number_to_currency current_user.limit
